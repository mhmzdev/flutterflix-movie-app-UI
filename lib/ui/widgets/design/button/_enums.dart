part of 'button.dart';

enum AppButtonStyle {
  primary,
  primaryDark,
  danger,
  grey,
}

enum AppButtonState {
  elevated,
  disabled,
  plain,
  bordered,
}

enum AppButtonRadius {
  less,
  medium,
  round,
  capsule,
}

enum AppButtonSize {
  tiny,
  mini,
  small,
  medium,
  large,
}
